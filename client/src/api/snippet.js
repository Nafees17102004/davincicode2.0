// import api from "./axiosInstance";

// const SnippetAPI = {
//   insertSnippet: (data) => api.post("/insertSnippet", data),
//   getSnippetById: (id) => api.get(`/getSnippetById/${id}`),
// };

// export default SnippetAPI;
